var assert = require('assert');
var commaSplit = require('..');

describe('comma-split', function() {

  it('should do something awesome', function() {
    assert.equal(commaSplit(), true);
  });
});
